# Database
# For local development (PostgreSQL)
# IMPORTANT: Add connection pool parameters for better reliability:
# - connection_limit: Max concurrent connections (10 for serverless, 20+ for traditional)
# - pool_timeout: Max seconds to wait for connection (default: 20)
# - connect_timeout: Max seconds to establish connection (default: 10)
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/capsuledb?connection_limit=20&pool_timeout=20&connect_timeout=10"

# Optional: example for remote database (must start with postgres:// or postgresql://)
# DATABASE_URL="postgresql://username:password@your-host:5432/your-db?connection_limit=10&pool_timeout=20&connect_timeout=10"

# For Vercel/serverless deployments, use smaller connection_limit:
# DATABASE_URL="postgresql://user:pass@host:5432/db?connection_limit=1&pool_timeout=20"

# Authentication (OTP via Twilio Verify)
TWILIO_ACCOUNT_SID="ACxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"
TWILIO_AUTH_TOKEN="xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"
TWILIO_VERIFY_SERVICE_SID="VAxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"
ADMIN_BOOTSTRAP_PHONE="+852XXXXXXXX"
ADMIN_BOOTSTRAP_CODE="your-secure-bootstrap-code-here"  # For bypassing SMS when Twilio blocks region
SESSION_SECRET="generate-32-bytes-base64"
DEVICE_TOKEN_SECRET="generate-32-bytes-base64"

# App Configuration
NEXT_PUBLIC_APP_NAME="EasyPack 膠囊配方管理系統"
NEXT_PUBLIC_APP_VERSION="1.0.0"

# AI Services
# OpenRouter API Key (for most AI features)
OPENROUTER_API_KEY="your-openrouter-api-key"

# Doubao SeeDream API Key (for image generation in marketing assistant)
# Get your API key from: https://ark.cn-beijing.volces.com/
DOUBAO_API_KEY="your-doubao-api-key"

# Optional: For production deployment
# NEXTAUTH_SECRET="your-secret-key"
# NEXTAUTH_URL="https://your-domain.com"
